# Android Game CV Autopilot environment template
# Шаблон переменных окружения Android Game CV Autopilot
#
# Copy to .env for local Docker runs. Never commit real .env files.
# Скопируй в .env для локального Docker. Реальный .env не коммить.

DASHBOARD_AUTH_ENABLED=1
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=change-me
DASHBOARD_MCP_API_KEY=change-me

# Vision/CV provider key. Keep empty until needed.
# Ключ Vision/CV провайдера. Оставь пустым, пока не нужен.
OPENROUTER_API_KEY=
CV_MODELS=xiaomi/mimo-v2.5
CV_MODEL_ATTEMPTS=3
CV_MAX_TOKENS=4096
CV_JSON_REPAIR_ATTEMPTS=1

# Local-first perception rollout.
# Shadow mode logs local providers without letting them drive actions.
# FRAME_SOURCE supports adb, adb_raw, screenrecord, replay, scrcpy, minicap.
# screenrecord needs host ffmpeg; scrcpy needs host scrcpy+ffmpeg; minicap needs device-side minicap files.
PERCEPTION_MODE=local_first
FRAME_SOURCE=adb
FRAME_SOURCE_INCLUDE_PNG=true
ACTION_MODE=menu
ENABLE_TEMPLATE_PROVIDER=true
ENABLE_UIAUTOMATOR_PROVIDER=true
ENABLE_LLM_FALLBACK=true
ENABLE_DETECTOR_PROVIDER=false
DETECTOR_MODEL_PATH=
DETECTOR_CONFIDENCE_THRESHOLD=0.50

# Device selection.
# Выбор устройства.
DEVICE_FARM=local
LOCAL_DEVICE=auto
ADB_SERVER_SOCKET=tcp:host.docker.internal:5037

# Remote farms. Fill only when used.
# Удаленные фермы. Заполняй только при использовании.
GENYMOTION_API_TOKEN=
BROWSERSTACK_USERNAME=
BROWSERSTACK_ACCESS_KEY=
LT_USERNAME=
LT_ACCESS_KEY=

# Safety defaults. Dashboard/Docker keep real purchases blocked.
# Безопасные значения. Dashboard/Docker блокируют реальные покупки.
PURCHASE_MODE=preview
GOOGLE_PHONE_MODE=manual
