[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    integration: mocked or live integration tests for ADB/Appium/provider boundaries
    live_adb: requires a connected local Android device or emulator
    live_openrouter: requires OPENROUTER_API_KEY and calls the configured Vision model
